[buildout]
extends =
    base.cfg
    ../versions/base-versions.cfg

newest = false

eggs +=
    collective.geo.usersmap


[config]
debug-mode = on
verbose-security = on
http-address = 8081


[instance]
environment-vars = ENABLE_PRINTING_MAILHOST True
